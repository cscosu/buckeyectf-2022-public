# crypto / megaxord
I took a Wikipedia article about Power Rangers and XOR'd the whole thing by one byte.
Since it's just one-byte XOR, there are only 256 possible keys (0-255), so the solution was to brute-force each possibility.

