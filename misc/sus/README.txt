# misc / sus
Pretty straightforward, the flag is repeated in the audio file using one-byte LSB.
The subtle audio differences relative to the original song tells you that it's LSB and not some visual diff.

